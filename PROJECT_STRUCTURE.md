# 项目结构说明

## 目录结构概览

```
qiejingxuan-art-studio-web/
│
├── 📁 app/                          # Next.js 15 App Router 应用目录
│   ├── 📁 [locale]/                 # 动态语言路由
│   │   ├── layout.tsx              # 多语言布局组件
│   │   ├── page.tsx                # 首页
│   │   ├── 📁 artists/             # 艺术家相关页面
│   │   │   ├── page.tsx            # 艺术家列表
│   │   │   └── 📁 [slug]/          # 动态路由
│   │   │       └── page.tsx        # 艺术家详情页
│   │   ├── 📁 works/               # 作品页面
│   │   │   └── page.tsx            
│   │   ├── 📁 commissions/         # 定制服务页面
│   │   │   └── page.tsx            
│   │   ├── 📁 about/               # 关于我们页面
│   │   │   └── page.tsx            
│   │   └── 📁 contact/             # 联系我们页面
│   │       └── page.tsx            
│   ├── globals.css                 # 全局样式
│   └── layout.tsx                  # 根布局
│
├── 📁 components/                   # React 可复用组件
│   ├── Header.tsx                  # 导航头部（带语言切换）
│   ├── Footer.tsx                  # 页脚组件
│   ├── LanguageSwitcher.tsx        # 语言切换器
│   ├── ArtistCard.tsx              # 艺术家卡片组件
│   ├── ArtworkCard.tsx             # 作品卡片组件
│   └── ContactForm.tsx             # 联系表单组件
│
├── 📁 lib/                          # 工具库和数据
│   ├── types.ts                    # TypeScript 类型定义
│   ├── 📁 data/                    # Mock 数据（待替换为 CMS）
│   │   ├── artists.ts              # 艺术家数据（3位）
│   │   └── artworks.ts             # 作品数据（9件）
│   └── 📁 utils/                   # 工具函数
│       └── placeholders.ts         # 占位图片生成器
│
├── 📁 messages/                     # 国际化翻译文件
│   ├── zh.json                     # 简体中文
│   ├── ja.json                     # 日本語
│   └── en.json                     # English
│
├── 📁 public/                       # 静态资源目录
│   └── 📁 images/                  
│       ├── 📁 artists/             # 艺术家头像
│       └── 📁 artworks/            # 作品图片
│
├── 📄 配置文件
│   ├── i18n.ts                     # next-intl 国际化配置
│   ├── middleware.ts               # Next.js 中间件（处理语言路由）
│   ├── next.config.ts              # Next.js 配置
│   ├── tailwind.config.ts          # Tailwind CSS 配置
│   ├── tsconfig.json               # TypeScript 配置
│   ├── postcss.config.mjs          # PostCSS 配置
│   └── package.json                # 项目依赖
│
└── 📄 文档文件
    ├── README.md                   # 项目主文档（英文+中文）
    ├── README.zh.md                # 详细开发文档（中文）
    ├── QUICK_START.zh.md           # 快速开始指南
    └── PROJECT_STRUCTURE.md        # 本文件

```

## 关键文件说明

### 配置文件

#### `next.config.ts`
- 配置 Next.js 15
- 集成 next-intl 插件
- 配置图片域名白名单

#### `middleware.ts`
- 处理语言路由重定向
- 默认语言：中文（zh）
- 支持的语言：zh, ja, en

#### `i18n.ts`
- 定义支持的语言列表
- 配置翻译文件加载逻辑

#### `tailwind.config.ts`
- 自定义颜色主题（紫色系主色调）
- 扩展间距系统
- 配置字体系统

### 核心组件

#### `Header.tsx`
**功能**：
- 响应式导航菜单
- 集成语言切换器
- 移动端汉堡菜单
- 品牌 Logo

**使用的语言键**：
- `nav.*` - 导航项名称

#### `Footer.tsx`
**功能**：
- 快速链接
- 社交媒体图标
- 版权信息

**使用的语言键**：
- `nav.*` - 导航链接
- `footer.*` - 页脚文本

#### `LanguageSwitcher.tsx`
**功能**：
- 下拉式语言选择器
- 显示语言国旗和名称
- 自动路由切换

#### `ArtistCard.tsx`
**Props**：
```typescript
{
  artist: Artist;
  locale: Locale;
}
```

**功能**：
- 展示艺术家头像、名字、国籍、简介
- 悬停效果
- 链接到详情页

#### `ArtworkCard.tsx`
**Props**：
```typescript
{
  artwork: Artwork;
  locale: Locale;
}
```

**功能**：
- 展示作品图片、标题、媒介、年份、尺寸
- 悬停效果
- 响应式布局

#### `ContactForm.tsx`
**功能**：
- 姓名、邮箱、留言输入
- 文件上传（UI）
- 表单验证
- 当前：Console 输出
- 未来：集成 Resend

### 数据结构

#### `lib/types.ts`

```typescript
// 多语言字符串
interface LocalizedString {
  zh: string;
  ja: string;
  en: string;
}

// 艺术家
interface Artist {
  slug: string;              // URL 标识符
  name: LocalizedString;     // 多语言名字
  avatar: string;            // 头像 URL
  nationality: string;       // 国籍
  bio: LocalizedString;      // 多语言简介
  artworks: string[];        // 作品 ID 列表
}

// 作品
interface Artwork {
  id: string;                // 唯一标识符
  title: LocalizedString;    // 多语言标题
  image: string;             // 图片 URL
  year: string;              // 创作年份
  medium: LocalizedString;   // 多语言媒介
  size: string;              // 尺寸
  artistSlug: string;        // 所属艺术家
}
```

#### `lib/data/artists.ts`

包含 3 位艺术家：
1. **张薇** (Zhang Wei) - 当代水墨艺术家
2. **山本武** (Takeshi Yamamoto) - 雕塑和装置艺术家
3. **陈艾玛** (Emma Chen) - 数字艺术家

导出函数：
- `getArtistBySlug(slug)` - 根据 slug 获取艺术家
- `getAllArtistSlugs()` - 获取所有 slug（用于静态生成）

#### `lib/data/artworks.ts`

包含 9 件作品（每位艺术家 3 件）

导出函数：
- `getArtworkById(id)` - 根据 ID 获取作品
- `getArtworksByArtist(slug)` - 获取特定艺术家的作品
- `getAllArtworks()` - 获取所有作品

### 页面结构

#### 首页 `/[locale]`
**sections**：
1. Hero - 大标题 + CTA
2. Featured Artists - 3位艺术家卡片
3. Featured Works - 6件作品展示
4. Philosophy - 品牌理念
5. CTA - 联系号召

#### 艺术家列表 `/[locale]/artists`
- 网格布局展示所有艺术家
- 使用 `ArtistCard` 组件

#### 艺术家详情 `/[locale]/artists/[slug]`
- 艺术家头像和详细信息
- 该艺术家的所有作品
- 静态生成所有可能的 slug

#### 作品集 `/[locale]/works`
- 网格布局展示所有作品
- 使用 `ArtworkCard` 组件

#### 定制服务 `/[locale]/commissions`
**sections**：
1. 流程说明（5步骤）
2. 服务优势（3项）
3. CTA 按钮

#### 关于我们 `/[locale]/about`
**sections**：
1. 品牌故事
2. 使命愿景
3. 核心价值观

#### 联系我们 `/[locale]/contact`
**sections**：
1. 联系表单
2. 联系方式信息
3. 地图占位

## 多语言系统

### 翻译键组织

```
messages/
├── zh.json
├── ja.json
└── en.json

结构：
{
  "nav": { ... },           // 导航
  "home": { ... },          // 首页
  "artists": { ... },       // 艺术家页面
  "works": { ... },         // 作品页面
  "commissions": { ... },   // 定制页面
  "about": { ... },         // 关于页面
  "contact": { ... },       // 联系页面
  "footer": { ... },        // 页脚
  "common": { ... }         // 通用文本
}
```

### 使用翻译

```typescript
import { useTranslations } from 'next-intl';

// 在组件中
const t = useTranslations('namespace');
const text = t('key');

// 嵌套键
const text = t('section.subsection.key');
```

## 样式系统

### Tailwind 自定义配置

**颜色**：
- Primary: 紫色系（#7c3aed 等）
- Neutral: 灰色系

**常用类**：
```css
/* 主按钮 */
bg-primary-600 hover:bg-primary-700

/* 卡片 */
bg-white rounded-lg shadow-md hover:shadow-xl

/* 容器 */
container mx-auto px-4 sm:px-6 lg:px-8

/* 网格布局 */
grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8
```

## 数据流

```
1. 页面组件请求数据
   ↓
2. 从 lib/data/ 导入 mock 数据
   ↓
3. 根据 locale 显示对应语言内容
   ↓
4. 渲染到页面

未来：
1. 页面组件请求数据
   ↓
2. 通过 Sanity Client 获取 CMS 数据
   ↓
3. 根据 locale 显示对应语言内容
   ↓
4. 渲染到页面
```

## 扩展点

### 当前是占位/Mock 的功能

1. **图片** - 使用 placehold.co
2. **表单提交** - Console 输出
3. **艺术家/作品数据** - 本地 TS 文件

### 预留的扩展架构

1. **CMS 集成**
   - `lib/sanity.ts` - Sanity 客户端配置
   - 环境变量配置

2. **邮件服务**
   - `app/api/contact/route.ts` - API 路由
   - Resend 集成

3. **SEO 优化**
   - Metadata 配置
   - Sitemap 生成
   - hreflang 标签

4. **图片 CDN**
   - Cloudinary / Vercel Image
   - 自动优化

## 性能优化

### 已实现

- ✅ Next.js Image 组件（自动优化）
- ✅ 静态生成（SSG）艺术家详情页
- ✅ 代码分割（自动）
- ✅ 字体优化

### 待实现

- ⏳ 图片懒加载配置
- ⏳ 预加载关键资源
- ⏳ CDN 配置
- ⏳ 缓存策略

## 开发工作流

### 本地开发

```bash
npm run dev      # 启动开发服务器
npm run build    # 构建生产版本
npm run start    # 运行生产版本
npm run lint     # 代码检查
```

### 添加新页面

1. 在 `app/[locale]/` 下创建目录
2. 创建 `page.tsx`
3. 在 `messages/*.json` 添加翻译
4. 在 `Header.tsx` 添加导航链接

### 添加新组件

1. 在 `components/` 创建组件文件
2. 定义 Props 接口
3. 导出组件
4. 在需要的页面导入使用

---

**维护者**：开发团队  
**最后更新**：2024年11月  
**版本**：Demo v0.1.0

